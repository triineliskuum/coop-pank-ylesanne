services:
  postgres:
    image: postgres:16
    container_name: coop-postgres
    environment:
      POSTGRES_DB: coopdb
      POSTGRES_USER: coopuser
      POSTGRES_PASSWORD: cooppass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: